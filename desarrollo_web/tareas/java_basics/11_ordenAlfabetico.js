/*Escribe una función que tome una lista de cadena de textos y devuelva una nueva lista con todas las cadenas en orden alfabético.*/
let lista = ['hola', 'adios', 'buenos dias', 'cuantas noches', 'devlyn'];
function ordenAlfabetico(arreglo){
    return arreglo.sort();
}
console.log(ordenAlfabetico(lista));